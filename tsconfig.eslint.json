{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": [
    "src/**/*.ts",
    "test/**/*.ts",
    "examples/**/*.ts",
    "vite.config.ts", // Incluye la configuración de Vite
    "*.cjs"
  ],
  "exclude": ["node_modules", "dist", "coverage/"]
}